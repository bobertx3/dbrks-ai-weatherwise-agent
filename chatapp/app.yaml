command: ["npm", "run", "start"]
runtime: nodejs20

env:
  - name: DATABRICKS_SERVING_ENDPOINT
    valueFrom: serving-endpoint
  # Dashboard: SQL warehouse for querying UC tables
  - name: DATABRICKS_SQL_WAREHOUSE_ID
    valueFrom: sql-warehouse
  # Ask Genie: Genie Space ID
  - name: DATABRICKS_GENIE_SPACE_ID
    value: "01f127dde0e31547b3e8cddf877cc672"
  # FEEDBACK RESOURCE: Uncomment to enable the feedback UI (requires experiment resource in databricks.yml)
  # - name: MLFLOW_EXPERIMENT_ID
  #   valueFrom: experiment
